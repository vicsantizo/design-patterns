export class Subsystem2 {
  // tranforms a string from being backward to forward
  reverseString(reversedString: string) {
    return reversedString.split('').reverse().join('');
  }

  // // More subsystem2 functionality....
}